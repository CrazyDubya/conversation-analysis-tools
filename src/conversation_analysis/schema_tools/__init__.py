"""schema_tools: LLM-gated SQL schema-change validator.

Quarantined subpackage: unrelated domain to conversation analysis; candidate for spin-out.
Requires requirements-optional.txt (openai, sqlparse).
"""
